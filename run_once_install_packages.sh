#!/bin/bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install coreutils curl fzf krew kubectx kubernetes-cli kubernetes-helm nvim openssl screenfetch terraform tfenv tldr tmux unzip zsh 
brew install --cask alacritty lens visual-studio-code slack workflowy 

